export enum displayEnum {
  OPEN = "open",
  CLOSE = "close",
}

export enum directionEnum {
  RIGHT = "right",
  LEFT = "left",
}

export enum pages {
  HOME = "/OC-P11-Kasa/",
  HOUSING = "/logement/",
  ABOUT = "/aPropos",
}
